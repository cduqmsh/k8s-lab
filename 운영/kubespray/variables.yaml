# kubeconfig 생성
kubeconfig_localhost: true
artifacts_dir: /kubespray/artifacts
unsafe_show_logs: false

# Cluster
kube_network_plugin: calico
kube_proxy_mode: ipvs

# Calico
calico_ipip_mode: Always
calico_vxlan_mode: Never
calico_network_backend: bird

kube_service_addresses: 10.233.0.0/18
kube_pods_subnet: 10.233.64.0/18
kube_network_node_prefix: 24

# Certificate
auto_renew_certificates: true

# Image GC
kubelet_image_gc_low_threshold: 60
kubelet_image_gc_high_threshold: 70

# HA
apiserver_loadbalancer_domain_name: 192.168.10.100

loadbalancer_apiserver:
  address: 192.168.10.100

####################################################
# Security Hardening
####################################################

authorization_modes:
  - Node
  - RBAC

kube_apiserver_request_timeout: 120s
kube_apiserver_service_account_lookup: true
kube_api_anonymous_auth: true

kubernetes_audit: true

audit_log_path: /var/log/kubernetes/audit.log
audit_log_maxage: 30
audit_log_maxbackups: 10
audit_log_maxsize: 100

tls_min_version: VersionTLS12

tls_cipher_suites:
  - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
  - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
  - TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305

kube_encrypt_secret_data: true

kube_encryption_resources:
  - secrets

kube_encryption_algorithm: secretbox

kube_profiling: false

####################################################
# ETCD
####################################################

etcd_metrics_port: 2381
etcd_deployment_type: kubeadm
etcd_quota_backend_bytes: 8589934592

####################################################
# Kubelet
####################################################

kubelet_rotate_server_certificates: true
kubelet_rotate_certificates: true
kube_read_only_port: 0
kubelet_authorization_mode_webhook: true
kubelet_authentication_token_webhook: true

####################################################
# Pod Security
####################################################

kube_pod_security_use_default: true
kube_pod_security_default_enforce: restricted

kube_pod_security_exemptions_namespaces:
  - kube-system
  - ingress-nginx
  - argocd
  - monitoring
  - logging

####################################################
# Private Registry Example
####################################################

containerd_registry_auth:
  - registry: harbor.example.local
    username: robot$kubelet
    password: CHANGE_ME

####################################################
# Proxy Example
####################################################

additional_no_proxy: harbor.example.local

####################################################
# DNS Example
####################################################

dns_etchosts: |
  192.168.10.100 argocd.example.local
  192.168.10.100 grafana.example.local
  192.168.10.100 prometheus.example.local
  192.168.10.100 alertmanager.example.local
  192.168.10.200 harbor.example.local
